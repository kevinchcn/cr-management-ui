{
  "name": "cr-management-ui",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 3000 --host 0.0.0.0",
    "start": "vite preview --port 3000 --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "^4.0.0"
  }
}